<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>{{ title }} - 11ty</title>
</head>
<body>
	<header>
		<ul>
			{% for item in collections.all %}
				<li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
			{% endfor %}
		</ul>
	</header>

	<h1>{{ title }}</h1>
	{{ content | safe }}
</body>
</html>